name: Discord Notification

on:
  workflow_call:
    inputs:
      status:
        description: 'Build status (success / failure / cancelled)'
        required: true
        type: string
      version:
        description: 'Build version'
        required: true
        type: string
      workflow-name:
        description: 'Workflow name'
        required: true
        type: string
      job-name:
        description: 'Job name that triggered notification'
        required: false
        type: string
        default: 'Build'
    secrets:
      DISCORD_WEBHOOK_URL:
        # オプション：未設定の場合は通知をスキップする（エラーにしない）
        required: false

jobs:
  notify:
    name: Send Discord Notification
    runs-on: ubuntu-latest

    # secrets は if 条件で直接参照できないため env 経由で空チェックを行う
    env:
      WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

    steps:
      - name: Check Webhook Availability
        id: check
        run: |
          if [ -n "$WEBHOOK_URL" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
            echo "ℹ️  DISCORD_WEBHOOK_URL が未設定のため通知をスキップします"
          fi

      - name: Prepare Notification Data
        id: prepare
        if: steps.check.outputs.available == 'true'
        run: |
          case "${{ inputs.status }}" in
            success)
              echo "color=3066993"   >> $GITHUB_OUTPUT
              echo "emoji=✅"        >> $GITHUB_OUTPUT
              echo "title=ビルド成功" >> $GITHUB_OUTPUT
              ;;
            failure)
              echo "color=15158332"  >> $GITHUB_OUTPUT
              echo "emoji=❌"        >> $GITHUB_OUTPUT
              echo "title=ビルド失敗" >> $GITHUB_OUTPUT
              ;;
            *)
              echo "color=15844367"         >> $GITHUB_OUTPUT
              echo "emoji=⚠️"               >> $GITHUB_OUTPUT
              echo "title=ビルドキャンセル"   >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Send Discord Notification
        if: steps.check.outputs.available == 'true'
        uses: tsickert/discord-webhook@v6.0.0
        with:
          webhook-url:       ${{ secrets.DISCORD_WEBHOOK_URL }}
          username:          "GitHub Actions"
          avatar-url:        "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
          embed-title:       "${{ steps.prepare.outputs.emoji }} ${{ steps.prepare.outputs.title }}: ${{ inputs.workflow-name }}"
          embed-color:       ${{ steps.prepare.outputs.color }}
          embed-url:         "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          embed-description: |
            **バージョン:** `${{ inputs.version }}`
            **ステータス:** ${{ inputs.status }}
            **ジョブ:** ${{ inputs.job-name }}
          embed-fields: |
            [
              {
                "name": "📦 リポジトリ",
                "value": "[${{ github.repository }}](${{ github.server_url }}/${{ github.repository }})",
                "inline": true
              },
              {
                "name": "🔀 ブランチ/タグ",
                "value": "`${{ github.ref_name }}`",
                "inline": true
              },
              {
                "name": "👤 実行者",
                "value": "${{ github.actor }}",
                "inline": true
              },
              {
                "name": "🔗 詳細",
                "value": "[ワークフローを確認](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})",
                "inline": false
              }
            ]
